# Action Cable usa WebSockets para comunicação em tempo real
# O adapter async funciona em memória para desenvolvimento

development:
  adapter: async

test:
  adapter: test

production:
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
